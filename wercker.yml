box: node
build:
  steps:
    - add-ssh-key:
        keyname: GITHUB
        host: github.com
    - add-ssh-key:
        keyname: GITHUB_KEY
        host: github.com
    - add-to-known_hosts:
        hostname: github.com
        fingerprint: SHA256:nThbg6kXUpJWGl7E1IGOCspRomTxdCARLviKw6E5SY8
    - script:
        name: temp
        code: |
          git clone git@github.com:exKAZUu/AIChallenge2018AtCodeFestival.git
    - script:
        name: show environment information
        code: |
          echo "node: $(node -v)"
          echo "yarn: $(yarn -v)"
    - script:
        name: yarn install
        code: |
          yarn config set cache-folder "$WERCKER_CACHE_DIR/yarn"
          yarn install
deploy:
  steps:
    - script:
        name: deploy docs
        code: |
          cd packages/docs
          # yarn gh-pages
